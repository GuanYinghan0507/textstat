[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    defect_probe: normative behavior expected to expose a candidate defect in 0.7.7
    compatibility: runs in a separate cmudict 1.1.3 environment
